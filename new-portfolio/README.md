# Pavlo Melnyk — Portfolio 2026

A new React and TypeScript portfolio built from the content and imagery in the existing `pavkot.github.io` site.

## Run locally

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
